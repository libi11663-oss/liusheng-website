import {sqliteTable,text,integer,index} from 'drizzle-orm/sqlite-core';
export const applications=sqliteTable('applications',{id:text('id').primaryKey(),name:text('name').notNull(),email:text('email').notNull(),city:text('city').notNull(),purpose:text('purpose').notNull(),story:text('story').notNull(),consentVersion:text('consent_version').notNull(),createdAt:integer('created_at').notNull()},t=>[index('idx_applications_email_created').on(t.email,t.createdAt)]);
